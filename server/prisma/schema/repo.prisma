model Repo {
  id        Int       @id @default(autoincrement())
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  name      String
  url       String
  username  String
  pwd       String
  branches  String?
  codePath  String?
  projects  Project[]
}
