# 复制为 .env 后按需修改。须与后端 config.yaml 的 encryption.key 一致（64 位 hex）。
# make dev / bun run dev 依赖本变量加密登录密码；嵌入二进制部署由服务端注入密钥，可不依赖本文件。
VITE_BUILDFLOW_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
